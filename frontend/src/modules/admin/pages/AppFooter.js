import React from "react";

export default function AppFooter() {
  return (
    <div style={{
      height: "40px",
      borderTop: "1px solid #eee",
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }}>
      <small>Organization Panel</small>
    </div>
  );
}
